specVersion: "v1beta"
serviceId: "kakeibo"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "kakeibo-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
